# render.yaml
# Render Blueprint Configuration for EduCore API (Free Tier)

services:
  # The Django Web API Service
  - type: web
    name: educore-api
    env: docker
    region: ohio
    plan: free
    dockerfilePath: docker/Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: educore-postgres
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: educore-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: educore-redis
          property: connectionString
      - key: DJANGO_SETTINGS_MODULE
        value: educore.settings.prod
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: SECURE_SSL_REDIRECT
        value: "True"
      - key: CORS_ALLOWED_ORIGINS
        # Allow frontend domains here (update later when UI is built)
        value: "https://your-frontend-domain.onrender.com"
      - key: DJANGO_ALLOWED_HOSTS
        # Allow the auto-generated render domain
        value: "educore-api.onrender.com"

  # Redis (for caching/background task queuing later)
  - type: redis
    name: educore-redis
    region: ohio
    plan: free
    ipAllowList: [] # Blocks all external access; allows internal Render access

databases:
  # PostgreSQL Database
  - name: educore-postgres
    region: ohio
    plan: free
